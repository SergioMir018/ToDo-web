import { TodoTask } from '../../../../packages/types/todo.types'

type Props = {
  task: TodoTask
}

export default function Task({ task }: Props) {
  return (
    <section>
      
    </section>
  )
}
